on: [push]

name: CI

jobs:

  tests:
    name: Tests (PHP ${{ matrix.php }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ['8.5']

    steps:
      - uses: actions/checkout@v7

      - uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none
          tools: composer

      - name: Get composer cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

      - name: Cache dependencies
        uses: actions/cache@v6
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-php${{ matrix.php }}-composer-${{ hashFiles('composer.json') }}
          restore-keys: ${{ runner.os }}-php${{ matrix.php }}-composer-

      - name: Cache phel compiled output
        uses: actions/cache@v6
        with:
          path: .phel/cache
          key: ${{ runner.os }}-php${{ matrix.php }}-phel-${{ hashFiles('src/**/*.phel', 'tests/**/*.phel', 'phel-config.php', 'composer.json') }}
          restore-keys: |
            ${{ runner.os }}-php${{ matrix.php }}-phel-

      - name: Install dependencies
        run: composer install --no-interaction --no-ansi --no-progress

      - name: Phel doctor
        run: vendor/bin/phel doctor

      - name: Full CI gate (format + lint + test + build)
        run: composer ci
