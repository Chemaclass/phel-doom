.idea/
.vscode/
.phel/
data/
out/
var/
vendor/

*.cache
composer.lock

# PHAR build artifact.
build/out/
.phel-repl-history
gacela*.php
phel-config-local.php

# .agents/ is generated by `vendor/bin/phel agent-install` (composer post-install).
# Regenerated from the vendored phel package; do not commit.
.agents/

# >>> agnostic-ai (managed) >>>
# Generated paths. Edit specs, not this block. Run `agnostic-ai sync` to refresh.
# Not committed: a fresh clone or `git worktree` lacks these until `agnostic-ai sync` runs (e.g. a post-checkout hook).
/.agnostic-ai/.sync-state
/.agnostic-ai/packs/
/.claude/CLAUDE.md
/.claude/README.md
/.claude/agent-memory-local/
/.claude/agents/
/.claude/rules/
/.claude/settings.json
/.claude/settings.local.json
/.claude/skills/
/AGENTS.md
/agnostic-ai.local.yaml
# <<< agnostic-ai (managed) <<<
# Impeccable design-detector local config
/.impeccable/

# Agent memory stays local, even when agnostic-ai (>= 0.66) stops ignoring it.
/.claude/agent-memory/
